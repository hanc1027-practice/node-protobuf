syntax = "proto2";
package Ecommerce;

message Product {
    required bool available = 1; // 是否上架（布林值）
    required string name = 2;    // 產品名稱（字串）
    required string desc = 3;    // 產品說明（字串）
    required float price = 4;    // 價格（浮點數）
}